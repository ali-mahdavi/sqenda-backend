security:
  oauth2:
    client:
      clientId: sqenda-service
      clientSecret: 1234
      accessTokenUri: http://localhost:8081/uaa/oauth/token
      grant-type: client_credentials
      scope: server

server:
  servlet:
    context-path: /sqenda
  port: 8082

feign:
  hystrix:
    enabled: true
